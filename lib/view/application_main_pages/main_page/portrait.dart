import 'package:flutter/material.dart';

class MainPortrait extends StatelessWidget {
  const MainPortrait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Main Portrait'),
      ),
    );
  }
}
