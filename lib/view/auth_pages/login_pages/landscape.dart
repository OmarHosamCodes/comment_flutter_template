import 'package:flutter/material.dart';

class LoginLandscape extends StatelessWidget {
  const LoginLandscape({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Login Landscape'),
      ),
    );
  }
}
