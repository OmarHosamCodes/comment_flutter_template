import 'package:flutter/material.dart';

class LoginPortriat extends StatelessWidget {
  const LoginPortriat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Login Portrait'),
      ),
    );
  }
}
