import 'package:flutter/material.dart';

class SignUpPortrait extends StatelessWidget {
  const SignUpPortrait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SignUp Portrait'),
      ),
    );
  }
}
