import 'package:flutter/material.dart';

class SignUpLanscape extends StatelessWidget {
  const SignUpLanscape({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SignUp Landscape'),
      ),
    );
  }
}
